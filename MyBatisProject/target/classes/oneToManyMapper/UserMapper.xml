<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="oneToManyMapper.UserMapper">
    <!--&lt;!&ndash;简单单表查询&ndash;&gt;-->
    <!--<select id="selectUserById" parameterType="int" resultType="po.User">-->
    <!--SELECT * FROM user WHERE id = #{id}-->
    <!--</select>-->

    <!--<select id="selectUserByName" parameterType="string" resultType="po.User">-->
    <!--SELECT * FROM user WHERE username like "%${value}%"-->
    <!--</select>-->

    <!--<insert id="insertUser" parameterType="po.User" useGeneratedKeys="true">-->
    <!--INSERT INTO user (username, sex, address) VALUES (#{username}, #{sex}, #{address})-->
    <!--</insert>-->

    <!--<update id="updateUserById" parameterType="po.User">-->
    <!--UPDATE user SET username = #{username}, sex = #{sex}, address = #{address}-->
    <!--WHERE id = #{id}-->
    <!--</update>-->

    <!-- 复杂查询 -->
    <select id="selectUserWithCarById" parameterType="int" resultMap="userAndCarMapper">
        SELECT * FROM user WHERE id = #{id}
    </select>
    <resultMap id="userAndCarMapper" type="po.User">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="sex" column="sex"/>
        <result property="address" column="address"/>
        <collection property="lessons" column="id" javaType="ArrayList"
                    ofType="po.Lesson" fetchType="lazy"
                    select="oneToManyMapper.LessonMapper.selectLessonByUserId">
            <id property="id" column="id"/>
            <result property="code" column="code"/>
            <result property="score" column="score"/>
        </collection>
    </resultMap>
</mapper>